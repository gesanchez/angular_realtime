import { TotalHeightDirective } from './total-height.directive';

describe('TotalHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new TotalHeightDirective();
    expect(directive).toBeTruthy();
  });
});
